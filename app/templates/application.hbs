<div class="container">
  <div class="menu">
    {{#link-to 'index'}}
      <h1>
        <em>SuperRentals</em>
      </h1>
    {{/link-to}}
    <div class="links">
      {{#link-to 'about'}}
        About
      {{/link-to}}
      {{#link-to 'contact'}}
        Contact
      {{/link-to}}
      
      {{#if isAuthenticated}}
        {{#link-to 'rentals.new-rental'}}
          New Rental
        {{/link-to}}
      {{/if}}

      {{#link-to 'login' class="right"}}
        Login
      {{/link-to}}
    </div>
  </div>
  <div class="body">
    {{outlet}}
  </div>
</div>
